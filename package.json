{
  "name": "thanks-i-hate-typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "nuke": "npm cache clean --force && rm -rf node_modules && rm -f package_lock.json && npm install && npm build",
    "build": "tsc -p .",
    "watch": "tsc -p . -w",
    "start": "node dist/src/main.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "github:discordjs/discord.js#master",
    "klasa": "github:dirigeants/klasa#master"
  },
  "devDependencies": {
    "@types/node": "^13.1.7",
    "@types/ws": "^7.2.0"
  }
}
